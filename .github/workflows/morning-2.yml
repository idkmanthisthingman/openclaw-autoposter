name: Morning Tweet 2
on:
  schedule:
    - cron: '5 1 * * *' # 06:35 IST
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - name: Restore cookies
        run: |
          if [ -n "$COOKIES_JSON" ]; then
            echo "$COOKIES_JSON" > state/cookies.json
          fi
        env:
          COOKIES_JSON: ${{ secrets.X_COOKIES }}

      - name: Post tweet 2
        run: node src/post.js --batch morning --index 1
        env:
          X_USERNAME: ${{ secrets.X_USERNAME }}
          X_PASSWORD: ${{ secrets.X_PASSWORD }}
          X_EMAIL: ${{ secrets.X_EMAIL }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}

      - name: Save cookies to artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cookies-morning-2
          path: state/cookies.json
          retention-days: 7
          if-no-files-found: ignore

      - name: Commit state
        run: |
          git config user.name "openclaw-bot"
          git config user.email "bot@openclaw.local"
          git add state/agent-state.json
          git diff --cached --quiet || git commit -m "Update state after morning tweet 2"
          git push
